#include<stdio.h>
int main()
{
	float length;
	float breadth;
	scanf("%f",&length);
	scanf("%f",&breadth);
	float area;
	area=length*breadth;
	printf("the area of the rectangle is %f",area);
	return 0;
	
}
